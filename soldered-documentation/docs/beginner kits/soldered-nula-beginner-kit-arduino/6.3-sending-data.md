---
slug: /soldered-nula-beginner-kit-arduino/sending-data
title: 6.3 Sending Data
id: nula-beginner-arduino-sending-data
hide_title: False
---

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Video demonstracije cijelog primjera" width="600px"/>

Cilj ovog primjera je pokazati kako **slati podatke s tvoje NULA MINI pločice na web poslužitelj** putem Wi-Fi mreže koristeći **HTTP POST metodu**.  
Koristit ćeš besplatni servis [**webhook.site**](https://webhook.site), koji ti omogućuje da odmah vidiš podatke koje uređaj šalje. 

<SuccessBox>
**U ovoj dokumentaciji naučit ćeš:**
- Kako spojiti NULA MINI na tvoju Wi-Fi mrežu.  
- Kako poslati POST zahtjev na web poslužitelj koristeći **HTTPClient** biblioteku.  
- Kako formatirati i poslati podatke s pločice.  
- Kako pratiti dolazne podatke putem [webhook.site](https://webhook.site).
</SuccessBox>

---

## Potreban hardver

- 1× Soldered NULA MINI pločica  
- 1× USB-C kabel  
- Wi-Fi mreža s pristupom internetu  

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Svi elementi potrebni za ovaj primjer" width="600px"/>

---

## Kako radi

U ovom primjeru NULA MINI se spaja na tvoju Wi-Fi mrežu i svakih 5 sekundi šalje nasumičan broj na udaljeni poslužitelj koristeći **HTTP POST** metodu.  
Svaki POST zahtjev sadrži polje `number` s vrijednošću između **0 i 100**.  
Na stranici [webhook.site](https://webhook.site) te zahtjeve vidiš u stvarnom vremenu, zajedno sa svim poslanim podacima.

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Pregled slanja podataka na webhook.site" width="600px"/>

---

## Postavljanje webhook.site

1. Otvori [**webhook.site**](https://webhook.site).  
2. Kopiraj **jedinstveni URL** prikazan na vrhu stranice, u obliku: `https://webhook.site/your-unique-id`  
3. Zamijeni vrijednost varijable `webhookURL` u svom kodu:
```cpp
const *char webhookURL = "https://webhook.site/your-unique-id";
``` 
<CenteredImage src="/img/under_construction.png" alt="sučelje webhook.site" caption="Webhook.site prikazuje primljene podatke" width="600px"/>

---

## POST zahtjev

Dok se **GET zahtjev** koristi za dohvaćanje informacija s poslužitelja, **POST zahtjev** koristimo za **slanje podataka** (očitanja senzora, podatke iz formi, statuse uređaja…).

---

## Kod

U nastavku je kompletan kod za ovaj primjer:
```cpp
#include <WiFi.h>
#include <HTTPClient.h>

const char* ssid = "Your_SSID";
const char* password = "Your_PASSWORD";

// Replace with your unique webhook.site URL
const char* webhookURL = "https://webhook.site/your-unique-id";

const unsigned long POST_INTERVAL = 5000; // 5 seconds
unsigned long lastPost = 0;

void setup() {
  Serial.begin(115200);
  Serial.println("Wi-Fi POST Request Example");

  WiFi.begin(ssid, password);
  Serial.print("Connecting to Wi-Fi");

  while (WiFi.status() != WL_CONNECTED) {
    delay(500);
    Serial.print(".");
  }

  Serial.println();
  Serial.println("Connected to Wi-Fi!");
  Serial.print("IP Address: ");
  Serial.println(WiFi.localIP());
}

void loop() {
  unsigned long now = millis();

  if (now - lastPost >= POST_INTERVAL) {
    lastPost = now;

    if (WiFi.status() == WL_CONNECTED) {
      HTTPClient http;

      // Generate random number (0–100)
      int randomNumber = random(0, 101);
      String postData = "number=" + String(randomNumber);

      Serial.println("----------------------------------");
      Serial.println("Sending POST request to webhook.site...");
      Serial.print("Data: ");
      Serial.println(postData);

      http.begin(webhookURL);
      http.addHeader("Content-Type", "application/x-www-form-urlencoded");
      int httpResponseCode = http.POST(postData);

      if (httpResponseCode > 0) {
        Serial.print("Server response code: ");
        Serial.println(httpResponseCode);
        String response = http.getString();
        Serial.println("Response body:");
        Serial.println(response);
      } else {
        Serial.print("POST failed. Error: ");
        Serial.println(http.errorToString(httpResponseCode));
      }

      http.end();
    } else {
      Serial.println("Wi-Fi not connected. Reconnecting...");
      WiFi.begin(ssid, password);
    }
  }
}

```

---

## Cijeli primjer

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Video demonstracije cijelog primjera" width="600px"/>

Pogledaj cijeli primjer koda na poveznici ispod:
<QuickLink title="6.3_Sending_Data.ino" description="Primjer koji prikazuje kako slati podatke s NULA MINI pločice na webhook.site koristeći HTTP POST zahtjev." url="https://github.com/SolderedElectronics/Soldered-NULA-Beginner-kit-Arduino-project-examples/blob/main/6_WiFi/6.3_Sending_Data/6.3_Sending_Data.ino" />
