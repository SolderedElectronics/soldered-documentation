---
slug: /soldered-nula-beginner-kit-arduino/led-traffic-light
title: 7.8 LED Semafor
id: nula-beginner-arduino-led-traffic-light
hide_title: False
---

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Demonstracija projekta LED Semafor" width="600px"/>

U ovom projektu izrađuješ **minijaturni semafor** koristeći tri LED diode i **NULA MINI** pločicu.  
LED diode automatski mijenjaju stanja između **zelene**, **blinkajuće zelene**, **narančaste**, **crvene** i **crvene + narančaste**, baš kao pravi semafor.  

Ovaj primjer uvodi pojam **konačnog automata (FSM – finite state machine)**, jednostavnog ali snažnog načina za upravljanje slijedom radnji u ugrađenim sustavima.

<SuccessBox>

**U ovoj dokumentaciji naučit ćeš:**
- kako simulirati **semafor** s više LED dioda  
- kako organizirati kod koristeći **konačni automat (FSM)**  
- kako koristiti `millis()` za **neblokirajuće mjerenje vremena** umjesto `delay()`  
- kako napraviti efekt **blinkajuće LED diode** pomoću vremenske logike  

</SuccessBox>

---

## Potreban hardver

- 1× Soldered NULA MINI pločica  
- 1× crvena LED dioda  
- 1× narančasta LED dioda  
- 1× zelena LED dioda  
- 3× otpornika od 330 Ω  
- breadboard  
- jumper žice  
- USB-C kabel  

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Svi elementi potrebni za LED Semafor projekt" width="600px"/>

---

## Kako radi

**Konačni automat (FSM)** je obrazac programiranja u kojem sustav prolazi kroz skup stanja, uvijek je u samo jednom stanju, a prelazi u drugo stanje na temelju vremena ili događaja.  

U ovom projektu LED diode prolaze kroz sljedeća stanja:

| **Stanje** | **LED diode koje svijetle** | **Trajanje** |
|------------|-----------------------------|--------------|
| Zeleno | 🟢 | 5 sekundi |
| Zeleno blinkanje | 🟢 (treperi) | 3 sekunde |
| Narančasto | 🟠 | 2 sekunde |
| Crveno | 🔴 | 5 sekundi |
| Crveno + narančasto | 🔴🟠 | 2 sekunde |

Nakon faze **crveno + narančasto**, semafor se vraća na **zeleno** i ciklus se stalno ponavlja.

Blinkanje zelene LED diode rješava se odvojenim brojačem vremena koji uključuje i isključuje LED svakih 0,4 sekunde, dok `millis()` prati koliko dugo svako stanje treba trajati.

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Koncept: konačni automat koji upravlja vremenom LED semafora" width="600px"/>

---

## Spajanje i ožičenje

| **Komponenta** | **NULA MINI pin** | **Opis** |
|----------------|------------------|----------|
| zelena LED dioda | IO2 | Zeleni signal semafora |
| narančasta LED dioda | IO3 | Narančasti signal semafora |
| crvena LED dioda | IO4 | Crveni signal semafora |
| sve katode LED dioda | GND | Zajednička masa |
| otpornici 220 Ω | u seriji sa svakom LED diodom | Ograničavanje struje za svaku LED diodu |

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Shema spajanja za LED Semafor projekt" width="600px"/>

---

## Cijeli kod

U nastavku je prikazan cijeli primjer koda za ovaj projekt:
```cpp 
const int LED_GREEN = 4;
const int LED_ORANGE = 3;
const int LED_RED = 2;

// Define the states of the traffic light
enum TrafficState {
  GREEN,
  GREEN_BLINK,
  ORANGE,
  RED,
  RED_ORANGE
};

TrafficState state = GREEN;
unsigned long lastChange = 0;
unsigned long stateDuration = 0;

// Blinking green timing
bool greenOn = true;
const unsigned long blinkInterval = 400; // 0.4 s blink interval
unsigned long lastBlink = 0;

void setup() {
  pinMode(LED_GREEN, OUTPUT);
  pinMode(LED_ORANGE, OUTPUT);
  pinMode(LED_RED, OUTPUT);

  // Start in green state
  state = GREEN;
  lastChange = millis();
  stateDuration = 5000; // stay green for 5 s
}

void loop() {
  unsigned long now = millis();

  switch (state) {

    case GREEN:
      digitalWrite(LED_GREEN, HIGH);
      digitalWrite(LED_ORANGE, LOW);
      digitalWrite(LED_RED, LOW);

      if (now - lastChange >= stateDuration) {
        state = GREEN_BLINK;
        lastChange = now;
        stateDuration = 3000; // blink green for 3 s
      }
      break;

    case GREEN_BLINK:
      digitalWrite(LED_ORANGE, LOW);
      digitalWrite(LED_RED, LOW);

      if (now - lastBlink >= blinkInterval) {
        greenOn = !greenOn;
        digitalWrite(LED_GREEN, greenOn);
        lastBlink = now;
      }

      if (now - lastChange >= stateDuration) {
        state = ORANGE;
        lastChange = now;
        stateDuration = 2000; // orange for 2 s
      }
      break;

    case ORANGE:
      digitalWrite(LED_GREEN, LOW);
      digitalWrite(LED_ORANGE, HIGH);
      digitalWrite(LED_RED, LOW);

      if (now - lastChange >= stateDuration) {
        state = RED;
        lastChange = now;
        stateDuration = 5000; // red for 5 s
      }
      break;

    case RED:
      digitalWrite(LED_GREEN, LOW);
      digitalWrite(LED_ORANGE, LOW);
      digitalWrite(LED_RED, HIGH);

      if (now - lastChange >= stateDuration) {
        state = RED_ORANGE;
        lastChange = now;
        stateDuration = 2000; // red + orange for 2 s
      }
      break;

    case RED_ORANGE:
      digitalWrite(LED_GREEN, LOW);
      digitalWrite(LED_ORANGE, HIGH);
      digitalWrite(LED_RED, HIGH);

      if (now - lastChange >= stateDuration) {
        state = GREEN;
        lastChange = now;
        stateDuration = 5000; // back to green for 5 s
      }
      break;
  }
}
```

---

## Cijeli primjer

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Video demonstracije kompletnog LED Semafor projekta" width="600px"/>

Pogledaj cijeli primjer koda na poveznici ispod:

<QuickLink title="7.8_LED_Traffic_Light.ino" description="Simulacija semafora pomoću NULA MINI pločice i tri LED diode. Pokazuje korištenje konačnih automata, neblokirajuće vremenske logike i upravljanje LED diodama." url="https://github.com/SolderedElectronics/Soldered-NULA-Beginner-kit-Arduino-project-examples/blob/main/7_Projects/7.8_LED_Traffic_Light/7.8_LED_Traffic_Light.ino" />