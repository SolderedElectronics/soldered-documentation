---
slug: /soldered-nula-beginner-kit-arduino/smart-weather-station
title: 7.1 Smart Weather Station
id: nula-beginner-arduino-smart-weather-station
hide_title: False
---

<CenteredImage src="/img/nula-beginner-kit-arduino/DSC00230.JPG" alt="pametna meteo stanica" caption="Qwiic shema spajanja za Smart Weather Station" width="1800px"/>

Cilj ovog projekta je izraditi **Smart Weather Station** koristeći **Soldered NULA MINI** pločicu.  
Stanica mjeri **temperaturu i vlagu** pomoću **SHTC3 senzora**, prikazuje očitanja u stvarnom vremenu na **LCD zaslonu** i šalje iste podatke na internet koristeći **Wi-Fi POST zahtjev** prema servisu [**webhook.site**](https://webhook.site).  
Ovo je tvoj prvi potpuni IoT (Internet of Things) projekt koji povezuje senzore, zaslon, mrežno povezivanje i komunikaciju s oblakom.

<SuccessBox>
**U ovoj dokumentaciji naučit ćeš:**
- Kako spojiti i koristiti **SHTC3 senzor temperature i vlage**.  
- Kako prikazivati živa očitanja na **LCD zaslonu** koristeći I²C komunikaciju.  
- Kako spojiti NULA MINI na Wi-Fi mrežu.  
- Kako slati podatke sa senzora na online servis koristeći **HTTP POST** zahtjev.  
</SuccessBox>

---

## Potreban hardver

- 1× Soldered NULA MINI pločica  
- 1× Soldered SHTC3 senzor temperature i vlage  
- 1× 16×2 Qwiic LCD zaslon  
- 2× Qwiic kabela  
- 1× USB-C kabel  

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Svi elementi potrebni za Smart Weather Station" width="600px"/>

---

## Kako radi

NULA MINI neprekidno mjeri temperaturu i vlagu pomoću **SHTC3 senzora**, zatim ta očitanja prikazuje na **16×2 LCD zaslonu**.  
Svakih 30 sekundi pločica šalje te podatke i na **webhook.site** kao **HTTP POST zahtjev**, a ti ih u pregledniku možeš pratiti u stvarnom vremenu.

---

## Spajanje i ožičenje

I **SHTC3 senzor** i **LCD zaslon** koriste **Qwiic (I²C)** spoj.  
Možeš ih jednostavno spojiti u lanac koristeći Qwiic kabele.  
Nije potrebno dodatno ožičenje, dovoljno je jedan Qwiic kabel s NULA MINI na senzor, pa drugi sa senzora na LCD.

| **BREAKOUT pločice** | **NULA MINI** |
|---|---|
| Qwiic | Qwiic |

<CenteredImage src="/img/nula-beginner-kit-arduino/DSC00225.JPG" alt="Qwiic ožičenje" caption="Qwiic shema spajanja za Smart Weather Station" width="600px"/>

---

## Postavljanje webhook.site

1. Otvori [**webhook.site**](https://webhook.site).  
2. Kopiraj **jedinstveni URL** prikazan na vrhu stranice, u formatu: `https://webhook.site/your-unique-id`  
3. Zamijeni vrijednost varijable `webhookURL` u svom kodu:
```cpp
const *char webhookURL = "https://webhook.site/your-unique-id";
``` 
<CenteredImage src="/img/under_construction.png" alt="sučelje webhook.site" caption="Webhook.site prikazuje primljene podatke" width="600px"/>

---

## Kod

U nastavku je kompletan primjer koda za ovaj projekt.

```cpp
#include <WiFi.h>
#include <HTTPClient.h>
#include "SHTC3-SOLDERED.h"
#include "LCD-SOLDERED.h"

const char* ssid = "Soldered";
const char* password = "dasduino";
const char* webhookURL = "https://webhook.site/42ff81ac-52d9-4cfa-95d4-ce933fee1f34";

SHTC3 shtc3;
LCD lcd(16, 2);

float temperature = 0.0;
float humidity = 0.0;
unsigned long lastUpdate = 0;
const unsigned long UPDATE_MS = 30000; // 30 seconds

void setup() {
  Serial.begin(115200);
  delay(500);

  lcd.begin();
  lcd.backlight();
  lcd.clear();
  lcd.setCursor(0, 0);
  lcd.print("Weather Station");
  lcd.setCursor(0, 1);
  lcd.print("Starting...");
  delay(1000);
  lcd.clear();

  if (!shtc3.begin()) {
    Serial.println("SHTC3 init failed!");
    lcd.print("SHTC3 error!");
    while (1) delay(100);
  }

  Serial.print("Connecting to Wi-Fi");
  lcd.setCursor(0, 0);
  lcd.print("Connecting WiFi");
  WiFi.begin(ssid, password);

  while (WiFi.status() != WL_CONNECTED) {
    delay(500);
    Serial.print(".");
    lcd.setCursor(0, 1);
    lcd.print(".");
  }

  Serial.println();
  Serial.println("Wi-Fi connected!");
  Serial.print("IP: ");
  Serial.println(WiFi.localIP());

  lcd.clear();
  lcd.print("WiFi Connected!");
  delay(800);
  lcd.clear();

  Serial.println("Smart Weather Station ready!");
}

void loop() {
  unsigned long now = millis();

  if (now - lastUpdate >= UPDATE_MS) {
    lastUpdate = now;

    shtc3.sample();
    temperature = shtc3.readTempC();
    humidity = shtc3.readHumidity();

    Serial.print("Temperature: ");
    Serial.print(temperature, 2);
    Serial.print(" °C, Humidity: ");
    Serial.print(humidity, 2);
    Serial.println(" %");

    lcd.clear();
    lcd.setCursor(0, 0);
    lcd.print("Temp: ");
    lcd.print(temperature, 1);
    lcd.print((char)223);
    lcd.print("C");

    lcd.setCursor(0, 1);
    lcd.print("Hum: ");
    lcd.print(humidity, 1);
    lcd.print(" %");

    if (WiFi.status() == WL_CONNECTED) {
      HTTPClient http;
      http.begin(webhookURL);
      http.addHeader("Content-Type", "application/x-www-form-urlencoded");

      String postData = "temperature=" + String(temperature, 2) + "&humidity=" + String(humidity, 2);
      int httpCode = http.POST(postData);

      if (httpCode > 0) {
        Serial.print("POST successful! Response code: ");
        Serial.println(httpCode);
      } else {
        Serial.print("POST failed. Error: ");
        Serial.println(http.errorToString(httpCode));
      }

      http.end();
    } else {
      Serial.println("Wi-Fi disconnected. Trying to reconnect...");
      WiFi.begin(ssid, password);
    }
  }
}

```

---

## Cijeli primjer

<CenteredImage src="/img/nula-beginner-kit-arduino/DSC00230.JPG" alt="Qwiic ožičenje" caption="Qwiic shema spajanja za Smart Weather Station" width="1800px"/>

Pogledaj cijeli primjer koda na poveznici ispod:
<QuickLink title="7.1_Smart_Weather_Station.ino" description="Projekt koji mjeri temperaturu i vlagu pomoću SHTC3 senzora, prikazuje podatke na LCD zaslonu i šalje očitanja na webhook.site svakih 30 sekundi." url="https://github.com/SolderedElectronics/Soldered-NULA-Beginner-kit-Arduino-project-examples/blob/main/7_Projects/7.1_Smart_Weather_Station/7.1_Smart_Weather_Station.ino" />
