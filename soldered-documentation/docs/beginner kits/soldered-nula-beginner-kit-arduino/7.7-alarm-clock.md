---
slug: /soldered-nula-beginner-kit-arduino/alarm-clock
title: 7.7 Alarm Clock
id: nula-beginner-arduino-alarm-clock
hide_title: False
---

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Demonstracija projekta Alarm Clock" width="600px"/>

U ovom projektu izradit ćeš **budilicu spojenu na Wi-Fi** koristeći **NULA MINI**, **Qwiic 16×2 LCD zaslon**, dva **tipkala** i **pasivni buzzer**.  
NULA MINI se spaja na tvoju Wi-Fi mrežu, dohvaća **trenutno vrijeme** s **NTP poslužitelja** i prikazuje ga na LCD zaslonu.  
Pomoću dva tipkala možeš postaviti sat i minute alarma. Kada se zadano vrijeme podudari s trenutačnim vremenom, buzzer počinje svirati sve dok ne pritisneš neko od tipkala.

Ovaj projekt pokazuje kako u jednom praktičnom primjeru spojiti **Wi-Fi mrežno povezivanje**, **sinkronizaciju vremena**, **ispis na LCD** i **ulaz preko tipkala**.

<SuccessBox>

**U ovoj dokumentaciji naučit ćeš:**
- Kako dobiti točno vrijeme koristeći **NTP (Network Time Protocol)**.  
- Kako prikazivati dinamičan tekst na **Qwiic LCD zaslonu** koristeći Soldered LCD biblioteku.  
- Kako koristiti **tipkala s vanjskim otpornicima** i ispravnim **debaunsiranjem** (debouncing).  
- Kako upravljati **pasivnim buzzerom** funkcijama `tone()` i `noTone()`.

</SuccessBox>

---

## Potreban hardver

- 1× Soldered NULA MINI pločica  
- 1× Soldered Qwiic 16×2 LCD zaslon  
- 2× tipkala  
- 2× otpornika od 10 kΩ  
- 1× pasivni buzzer  
- breadboard  
- jumper žice  
- Qwiic kabel  
- USB-C kabel  

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Svi elementi potrebni za Alarm Clock projekt" width="600px"/>

---

## Kako radi

Nakon uključivanja, NULA MINI se spaja na tvoju Wi-Fi mrežu i dohvaća trenutno vrijeme s **NTP poslužitelja** `pool.ntp.org`.  
Na **LCD zaslonu** se prikazuju i **trenutno vrijeme** i **vrijeme alarma** koje podešavaš pomoću dva tipkala:

- **Tipkalo 1:** povećava **sat** alarma.  
- **Tipkalo 2:** povećava **minute** alarma.  

Svako tipkalo koristi **vanjski pull-down otpornik** i **debaunsiranje** pomoću mjerenja vremena kako bi očitanja bila pouzdana.  
Kada se vrijeme na satu poklopi s postavljenim vremenom alarma, **pasivni buzzer** generira ponavljajući ton sve dok ne pritisneš bilo koje tipkalo, čime zaustavljaš alarm.

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Koncept: NULA MINI dohvaća vrijeme preko Wi-Fi mreže i aktivira alarm u zadano vrijeme" width="600px"/>

---

## Spajanje i ožičenje

| **Komponenta** | **NULA MINI pin** | **Opis** |
|----------------|------------------|----------|
| Qwiic LCD | Qwiic port | Prikazuje vrijeme i postavke alarma |
| Tipkalo 1 (sat) | IO2 | Povećava sat alarma (aktivno na HIGH) |
| Tipkalo 2 (minute) | IO3 | Povećava minute alarma (aktivno na HIGH) |
| Buzzer (pasivni) | IO4 | Izlaz tona pomoću funkcije `tone()` |
| Otpornik 10 kΩ (×2) | IO2→GND, IO3→GND | Vanjski pull-down otpornici za stabilna očitanja tipkala |
| 3.3 V / GND | — | Napajanje i masa sustava |

<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Shema spajanja Wi-Fi budilice s LCD zaslonom, tipkalima i pasivnim buzzerom" width="600px"/>

---

## Cijeli kod

U nastavku je kompletan primjer koda za ovaj projekt:
```cpp
#include "LCD-SOLDERED.h"
#include <WiFi.h>
#include <time.h>

const char* WIFI_SSID = "your ssid";
const char* WIFI_PASS = "your password";

const char* ntpServer = "pool.ntp.org";
const long gmtOffset_sec = 0;     // Adjust to your timezone
const int daylightOffset_sec = 0; // Set to 3600 if you have DST

LCD lcd(16, 2); // Qwiic 16x2 LCD

const int BUTTON_HOUR = 2;  // Hour set button
const int BUTTON_MIN  = 3;  // Minute set button
const int BUZZER_PIN  = 4;  // Passive buzzer pin

int alarmHour = 7;
int alarmMinute = 0;
bool alarmTriggered = false;

// Debounce logic
bool lastHourState = LOW;
bool lastMinState = LOW;
unsigned long lastHourChangeMs = 0;
unsigned long lastMinChangeMs = 0;
const unsigned long debounceMs = 25;

unsigned long lastSync = 0;
const unsigned long syncInterval = 60000; // Sync every 60 seconds

void getLocalTimeData(struct tm *timeinfo) {
  if (!getLocalTime(timeinfo)) {
    Serial.println("Failed to obtain time from NTP");
  }
}

// Play tone on passive buzzer
void beepAlarm() {
  for (int i = 0; i < 5; i++) {
    tone(BUZZER_PIN, 1000); // 1 kHz tone
    delay(300);
    noTone(BUZZER_PIN);
    delay(100);
  }
}

void setup() {
  Serial.begin(115200);

  lcd.begin();
  lcd.backlight();

  pinMode(BUZZER_PIN, OUTPUT);
  pinMode(BUTTON_HOUR, INPUT); // external pull-down
  pinMode(BUTTON_MIN, INPUT);  // external pull-down

  lcd.clear();
  lcd.setCursor(0, 0);
  lcd.print("Connecting WiFi");

  WiFi.begin(WIFI_SSID, WIFI_PASS);
  while (WiFi.status() != WL_CONNECTED) {
    delay(500);
    Serial.print(".");
  }

  Serial.println("\nWiFi connected!");
  lcd.clear();
  lcd.print("WiFi connected");
  delay(1000);

  configTime(gmtOffset_sec, daylightOffset_sec, ntpServer);
  lcd.clear();
  lcd.print("Time synced!");
  delay(1000);
  lcd.clear();
}

void loop() {
  struct tm timeinfo;
  getLocalTimeData(&timeinfo);

  // Display current time
  char timeStr[6];
  sprintf(timeStr, "%02d:%02d", timeinfo.tm_hour, timeinfo.tm_min);
  lcd.setCursor(0, 0);
  lcd.print("Time: ");
  lcd.print(timeStr);

  // Display alarm
  lcd.setCursor(0, 1);
  lcd.print("Alarm ");
  if (alarmHour < 10) lcd.print("0");
  lcd.print(alarmHour);
  lcd.print(":");
  if (alarmMinute < 10) lcd.print("0");
  lcd.print(alarmMinute);

  // Handle hour button
  bool hourReading = digitalRead(BUTTON_HOUR);
  unsigned long now = millis();
  if (hourReading != lastHourState && (now - lastHourChangeMs) > debounceMs) {
    lastHourChangeMs = now;
    if (lastHourState == LOW && hourReading == HIGH) {
      alarmHour++;
      if (alarmHour > 23) alarmHour = 0;
    }
    lastHourState = hourReading;
  }

  // Handle minute button
  bool minReading = digitalRead(BUTTON_MIN);
  if (minReading != lastMinState && (now - lastMinChangeMs) > debounceMs) {
    lastMinChangeMs = now;
    if (lastMinState == LOW && minReading == HIGH) {
      alarmMinute++;
      if (alarmMinute > 59) alarmMinute = 0;
    }
    lastMinState = minReading;
  }

  // Check if it's time for alarm
  if (timeinfo.tm_hour == alarmHour && timeinfo.tm_min == alarmMinute && !alarmTriggered) {
    alarmTriggered = true;
    lcd.clear();
    lcd.setCursor(0, 0);
    lcd.print("ALARM!");
    Serial.println("Alarm Triggered!");
    beepAlarm();
  }

  // Stop alarm if button pressed
  if (alarmTriggered && (hourReading == HIGH || minReading == HIGH)) {
    alarmTriggered = false;
    lcd.clear();
  }

  // Re-sync time occasionally
  if (millis() - lastSync > syncInterval) {
    configTime(gmtOffset_sec, daylightOffset_sec, ntpServer);
    lastSync = millis();
  }

  delay(100);
}
```
---

## Cijeli primjer
<CenteredImage src="/img/under_construction.png" alt="privremena slika" caption="Video demonstracije kompletnog Alarm Clock projekta" width="600px"/>

Pogledaj cijeli primjer koda na poveznici ispod:

<QuickLink title="7.7_Alarm_Clock.ino" description="Wi-Fi budilica temeljena na NULA MINI pločici, Qwiic LCD zaslonu, tipkalima s debaunsiranjem i pasivnom buzzeru. Dohvaća vrijeme preko NTP-a i aktivira alarm u zadano vrijeme." url="https://github.com/SolderedElectronics/Soldered-NULA-Beginner-kit-Arduino-project-examples/blob/main/7_Projects/7.7_Alarm_Clock/7.7_Alarm_Clock.ino" /> ```